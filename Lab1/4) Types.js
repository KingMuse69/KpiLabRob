
let num = 42;              // number
let text = "Hello";        // string
let isCool = true;         // boolean
let list = [1, 2, 3];      // array
let person = { name: "Alex", age: 20 }; // object

console.log(typeof num, typeof text, typeof isCool, typeof list, typeof person);
